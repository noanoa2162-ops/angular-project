import { Injectable } from '@angular/core';

/**
 * NOTE: The /api/users endpoint does NOT exist on the teacher's server.
 * To add team members, use their User ID directly (they need to share it with you).
 * This service is kept as a placeholder for potential future use.
 */
@Injectable({
  providedIn: 'root'
})
export class UsersService {
  // No methods available - /api/users endpoint doesn't exist on the server
}
